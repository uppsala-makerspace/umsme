import Login from './Login';

export default {
  title: 'UMSAPP/Login',
  component: Login,
  parameters: {},
  tags: ['autodocs']
};

const method = () => {};

export const LoginPage = {
  args: {
    google: {method}
  }
};