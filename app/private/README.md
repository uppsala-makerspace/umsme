# Certificates for Swish

## Testing

For testing purposes use the `Swish_test_private_key.key` and `Swish_test_public_key.pem` together with the `Swish_TLS_RootCA.pem`.
The private and public keys correspond to the swish number `1234679304`.
